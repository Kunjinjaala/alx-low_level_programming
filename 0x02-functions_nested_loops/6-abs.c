#include "main.h"

/**
 * _abs - computes the absolute value of an integer
 *
 * @c: The number to computed
 *
 *Return: Always 0 on success
 */

int _abs(int c)
{
	while
	(c < 0)
	{
	int abs_val;

	abs_val = c * -1;

	return (abs_val);
	}
	return (c);
}
